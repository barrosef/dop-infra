apiVersion: v1
kind: ConfigMap
metadata:
  name: nats-config
data:
  nats.conf: |
    port: 4222
    http: 8222          # monitoramento

    jetstream {
      store_dir: "/data/jetstream"
      max_memory_store: 256MB
      max_file_store: 4GB
    }
