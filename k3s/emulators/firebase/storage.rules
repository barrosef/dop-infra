rules_version = '2';
// The LOCAL EMULATOR's rules. In production, access to the bucket is always
// mediated by the core through a signed URL — the client never talks directly.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
